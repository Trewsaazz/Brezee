<Window x:Class="Brezee.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonDock="https://github.com/Dirkster99/AvalonDock"
        xmlns:avalonDockControls="clr-namespace:AvalonDock.Controls;assembly=AvalonDock"
        xmlns:avalonDockConverters="clr-namespace:AvalonDock.Converters;assembly=AvalonDock"
        xmlns:commands="clr-namespace:Brezee.App.Commands"
        xmlns:avalonDockThemes="clr-namespace:AvalonDock.Themes;assembly=AvalonDock.Themes.VS2013"
        xmlns:shell="clr-namespace:Brezee.App.Shell"
        xmlns:res="clr-namespace:Brezee.App.Resources"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance shell:ShellViewModel}"
        Title="{x:Static res:Strings.App_Name}"
        Width="1280" Height="800"
        MinWidth="640" MinHeight="400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <avalonDockConverters:BoolToVisibilityConverter x:Key="BoolToVisibility" />

        <!-- Menu items bound to an AppCommand take their text and shortcut from it. -->
        <Style TargetType="{x:Type MenuItem}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Command, RelativeSource={RelativeSource Self}, Converter={x:Static commands:IsAppCommandConverter.Instance}}" Value="True">
                    <Setter Property="Header" Value="{Binding Command.Text, RelativeSource={RelativeSource Self}}" />
                    <Setter Property="InputGestureText" Value="{Binding Command.GestureText, RelativeSource={RelativeSource Self}}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="{x:Static res:Strings.Menu_File}">
                <MenuItem Command="{Binding Commands[file.exit]}" />
            </MenuItem>
            <MenuItem Header="{x:Static res:Strings.Menu_View}">
                <MenuItem Command="{Binding Commands[view.explorer]}" />
                <MenuItem Command="{Binding Commands[view.output]}" />
                <Separator />
                <MenuItem Command="{Binding Commands[view.welcome]}" />
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="{x:Static res:Strings.Status_Ready}" />
            <StatusBarItem HorizontalAlignment="Right"
                           Content="{Binding CoreVersion}"
                           ContentStringFormat="{x:Static res:Strings.App_CoreVersion}" />
        </StatusBar>

        <avalonDock:DockingManager AnchorablesSource="{Binding Tools}"
                                   DocumentsSource="{Binding Documents}"
                                   ActiveContent="{Binding ActiveDocument, Mode=TwoWay, Converter={x:Static shell:ActiveDocumentConverter.Instance}}">

            <avalonDock:DockingManager.Theme>
                <avalonDockThemes:Vs2013LightTheme />
            </avalonDock:DockingManager.Theme>

            <avalonDock:DockingManager.LayoutUpdateStrategy>
                <shell:ToolLayoutStrategy />
            </avalonDock:DockingManager.LayoutUpdateStrategy>

            <avalonDock:DockingManager.LayoutItemContainerStyleSelector>
                <shell:PaneStyleSelector>
                    <shell:PaneStyleSelector.ToolStyle>
                        <Style TargetType="{x:Type avalonDockControls:LayoutAnchorableItem}">
                            <Setter Property="Title" Value="{Binding Model.Title}" />
                            <Setter Property="ContentId" Value="{Binding Model.ContentId}" />
                            <Setter Property="CanClose" Value="False" />
                            <Setter Property="IsActive" Value="{Binding Model.IsActive, Mode=TwoWay}" />
                            <Setter Property="Visibility"
                                    Value="{Binding Model.IsVisible, Mode=TwoWay, Converter={StaticResource BoolToVisibility}, ConverterParameter={x:Static Visibility.Hidden}}" />
                        </Style>
                    </shell:PaneStyleSelector.ToolStyle>
                    <shell:PaneStyleSelector.DocumentStyle>
                        <Style TargetType="{x:Type avalonDockControls:LayoutDocumentItem}">
                            <Setter Property="Title" Value="{Binding Model.Title}" />
                            <Setter Property="ContentId" Value="{Binding Model.ContentId}" />
                            <Setter Property="CloseCommand" Value="{Binding Model.CloseCommand}" />
                        </Style>
                    </shell:PaneStyleSelector.DocumentStyle>
                </shell:PaneStyleSelector>
            </avalonDock:DockingManager.LayoutItemContainerStyleSelector>

            <!-- Layout skeleton. ToolLayoutStrategy drops each tool into its named pane. -->
            <avalonDock:LayoutRoot>
                <avalonDock:LayoutPanel Orientation="Horizontal">
                    <avalonDock:LayoutAnchorablePane Name="LeftToolPane" DockWidth="280" />
                    <avalonDock:LayoutPanel Orientation="Vertical">
                        <avalonDock:LayoutDocumentPane />
                        <avalonDock:LayoutAnchorablePane Name="BottomToolPane" DockHeight="180" />
                    </avalonDock:LayoutPanel>
                </avalonDock:LayoutPanel>
            </avalonDock:LayoutRoot>
        </avalonDock:DockingManager>
    </DockPanel>
</Window>
